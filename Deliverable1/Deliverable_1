using System;

namespace Deliverable1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("How many people are we making PB&J sandwiches for?");
            string numinput = Console.ReadLine();

            int num = 0;

            bool success = Int32.TryParse(numinput, out num);

            Console.WriteLine("We will need: " +
                "");

            if (success)
            {
                int s = (num * 2);
                Console.WriteLine(l + " loaves of bread.");
                int tb = (num * 2);
                Console.WriteLine(tb + " tablespoons of peanut butter.");
                int tsp = (num * 2);
                Console.WriteLine(tsp + " teaspoons of jelly.");
            }
            else
            {
                Console.WriteLine("Please enter a valid number and try again.");
            }
            Console.WriteLine("...or...");
            if (success)
            {
                double l = (num*2)/ 28;
                Console.WriteLine(l + " slices of bread.");
                double jp = (num * 2)/32;
                Console.WriteLine(jp + " jars of peanut butter.");
                double jj = (num * 2)/48;
                Console.WriteLine(jj + " jars of jelly.");
            }
            else
            {
                Console.WriteLine("Error.");
            }
            if (success)
            {
                Console.WriteLine("Would you like to try again? Enter yes or y to continue, or enter any other key to exit.");
                string input = Console.ReadLine();
                char chr;
                bool val;
              
                bool result = Char.TryParse(String backTop, out char char_variable);

                bool backTop = true;

                while (backTop)
                { int char = false}

                Console.WriteLine("Goodbye.");
            }
    }
    }
}